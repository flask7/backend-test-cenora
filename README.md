# backend-test-cenora

- STEPS TO EXECUTE:
    1- Install webdriver and add to path, if you`re using Windows, in C: directory, MacOS and Linux the path to installation is: /usr/local/bin
    2- Execute npm install
    3- Execute node app.js